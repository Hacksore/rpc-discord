name: CI

on:
  push:
  pull_request:

jobs:
   build:
    name: Cargo Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/rust-toolchain@master
      - run: cargo build